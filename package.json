{
  "name": "chat-app",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install --prefix backend && npm install --prefix frontend && npm run build --prefix frontend --output public",
    "start": "concurrently \"npm run install-backend\" \"npm run install-frontend\" \"npm run start-backend\" \"npm run start-frontend\"",
    "install-backend": "npm install --prefix backend",
    "install-frontend": "npm install --prefix frontend",
    "start-backend": "npm run start --prefix backend",
    "start-frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
